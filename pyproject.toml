[project]
name = "physics191b-capstone"
version = "0.1.0"
description = "Capstone experiments (QCT-style surface-code decoder, etc.)"
requires-python = ">=3.11"
dependencies = [
    "numpy>=1.26",
    "torch>=2.2",
    "stim>=1.14",
]
